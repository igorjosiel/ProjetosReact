/*import { ACRESCENTA, SUBTRAI, ALTERA_NOME } from "./type";

export const alterarNomeUsuario = (params, callback = () => null) => {
    return dispatch => {
        dispatch({ type: ALTERA_NOME, payload: { nomeUsuario: "" } });
    }
}

export const acrescentaValor = (params, callback = () => null) => {
    return dispatch => {
        dispatch({ type: ACRESCENTA, payload: { valorPontos: valorPontos +1 } });
    }
}

export const subtraiValor = (params, callback = () => null) => {
    return dispatch => {
        dispatch({ type: SUBTRAI, payload: { valorPontos: valorPontos -1 } });
    }
}*/